// Shared client-side state for the app.
//
// The persisted `filter` store used to drive the `/apps` page toggle
// (All Apps / Completed). Every app in the catalog is now shipped, so
// the toggle was redundant and was removed during the catalog prune
// (see ai-generated/2026-08-09-share-target-applinks-e2ee-sync-plan.md, Task 8).
